import Button from '@/client/common/components/lib/Button';

const JoinElectricSystemsMenuItem = () => {
  return <Button onClick={() => {}}>Join electric systems</Button>;
};

export default JoinElectricSystemsMenuItem;
