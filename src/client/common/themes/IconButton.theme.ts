import { ComponentStyleConfig } from '@chakra-ui/theme';

const IconButtonTheme: ComponentStyleConfig = {
  baseStyle: {
    backgroundColor: 'blue',
    padding: '4px',
  },
};

export default IconButtonTheme;
