// #include "./shear_vertical.h"
#include "./rotate.h"


namespace spright
{
namespace editor
{
    void rotate(TileLayer &source, DocumentFactory *documentFactory, const BoundsInt &bounds, float angle)
    {
        // TileLayer temp1 =
        //     documentFactory->createUserLayer(source.getBounds(), "", source.getTileSize());

        // TileLayer temp2 =
        //     documentFactory->createUserLayer(source.getBounds(), "", source.getTileSize());

        // TileLayer temp3 =
        //     documentFactory->createUserLayer(source.getBounds(), "", source.getTileSize());

        // float hangle = atan(sin(angle));

        // shear_horizontal(temp1, temp2, bounds, angle);
        // shear_vertical(source, temp1, bounds, angle);
        // // shear_vertical(temp2, temp3, bounds, angle / 2.0f);

        // source.clear();

        // for (Rect2D *tile : temp3.getTiles())
        // {
        //     source.add(*tile);
        // }
    }
} // namespace editor
} // namespace spright
