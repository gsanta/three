#include "intersect_ray_box.h"

namespace spright
{
namespace engine
{
    bool intersect_ray_box(const Box &box, const Ray3 &ray)
    {
        bool inside = true;
    }
} // namespace engine
} // namespace spright
